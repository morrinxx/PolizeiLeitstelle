export class Operation {
  constructor(
    public id: Number,
    public name: String,
    public type: String,
    public autoId: string,
    public district: string,
    public timestamp: string
  ) {}
}
